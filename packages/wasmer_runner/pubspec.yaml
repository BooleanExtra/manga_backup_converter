name: wasmer_runner
description: Wasmer-backed WASM runner implementing the wasm_runner interface.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  archive: ^4.0.0
  code_assets: ^1.0.0
  crypto: ^3.0.0
  ffi: ^2.2.0
  hooks: ^1.0.0
  http: ^1.2.0
  wasm_runner:
    path: ../wasm_runner

dev_dependencies:
  app_lints:
    path: ../app_lints
  checks: ^0.3.0
  ffigen: ^20.1.1
  test: ^1.24.0

platforms:
  ios:
  android:
  windows:
  macos:
  linux:
